# Mnemosyne — required environment variables
# Copy to .env and fill in values

# Anthropic API key — used by distiller (extract training pairs) and querier (RAG answers)
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...
