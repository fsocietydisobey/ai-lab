# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Training artifacts (large / generated — reproduce with scripts/)
# downloaded base models (HF cache)
models/
# trained LoRA adapters + merged models
adapters/
# assembled CPT corpora
corpora/
# TensorBoard event files (tensorboard --logdir runs/)
runs/
# distilled training pairs (JSONL per domain)
data/
